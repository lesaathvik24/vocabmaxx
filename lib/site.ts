export const siteUrl = (process.env.NEXT_PUBLIC_APP_URL ?? 'https://vocabmaxx.com').replace(/\/+$/, '')

export const siteName = 'VocabMaxx'

export const siteTagline = 'Capture words, own them.'

export const siteDescription =
    'Type a word — get a clean definition and two real-world examples in ~200ms. SM-2 spaced repetition brings it back right before you forget. Your vocabulary lives in your own database, exportable any time.'
